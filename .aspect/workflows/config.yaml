# See https://docs.aspect.build/v/workflows/config
---
workspaces:
  - .:
      tasks:
        - test:
  - docs:
      tasks:
        - test:
  - example

tasks:
  - test:
  - test:
      name: "Test (WORKSPACE)"
      bazel:
        flags:
          - --noenable_bzlmod
          - --enable_workspace
notifications:
  github: {}
