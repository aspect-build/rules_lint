# See https://docs.aspect.build/v/workflows/config
---
workspaces:
  - .:
      tasks:
        - workspace:
            without: true
  - docs:
      tasks:
        - workspace:
            without: true
  - example

tasks:
  - test:
  - test:
      name: "Test (WORKSPACE)"
      id: workspace
      bazel:
        flags:
          - --noenable_bzlmod
          - --enable_workspace
notifications:
  github: {}
