matrix:
  platform:
    - debian10
    - macos
  bazel: [8.5.0, 9.x]
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "@aspect_rules_lint//lint:all"
      - "@aspect_rules_lint//format:all"
