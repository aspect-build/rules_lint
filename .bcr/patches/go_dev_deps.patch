--- a/MODULE.bazel
+++ b/MODULE.bazel
@@ -34,7 +34,7 @@ use_repo(multitool, "multitool")
 
 # To allow /tools to be built from source
 # NOTE: when publishing to BCR, we patch this to be False, as we publish pre-built binaries with our releases.
-IS_PRERELEASE = True
+IS_PRERELEASE = False
 
 bazel_dep(
     name = "toolchains_protoc",
